\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{float}
\usepackage{comment}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref} % Hide link boxes
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{tcolorbox} 
\usepackage{fancyhdr} % Aggiunto per intestazioni personalizzate

% Configuration for listings codeBlock 
\lstset{
  basicstyle=\ttfamily\small,
  keywordstyle=\color{blue},
  commentstyle=\color{green!50!black},
  stringstyle=\color{red},
  showstringspaces=false,
  breaklines=true,
  frame=single, 
  numbers=left, % Place row numbers on the left
  numberstyle=\tiny\color{gray},  
  stepnumber=1, 
  numbersep=-5pt, % Distance from the row numbers and text
  tabsize=2, % Tab space
  captionpos=b, % Caption positioning
}

% Header configuration
\pagestyle{fancy}
\fancyhf{} % Clear header and footer
\fancyhead[L]{\leftmark} % Current section on the left
\fancyhead[R]{\thepage} % Page number on the right
 
\newcommand{\definition}[2]{%
  \paragraph{#1:} #2%
}

\newcommand{\defterm}[2]{%
  \paragraph{\textbf{#1}:} #2%
}

% List of definitions
\newenvironment{definitions}{%
  \begin{description}[font=\normalfont\bfseries\large, leftmargin=0pt, labelindent=0pt]
}{%
  \end{description}
}

% Definition with colored box
\newtcolorbox{defbox}[1]{
  colback=blue!5!white,
  colframe=blue!75!black,
  title=#1,
  fonttitle=\bfseries
}

